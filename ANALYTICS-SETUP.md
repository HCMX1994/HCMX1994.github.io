# Citation statistics

## Google Scholar

The initial snapshot (595 citations, h-index 12, i10-index 12) was read from the user's open profile on 24 September 2026. `scripts/update_scholar.py` fetches the same public profile once per run. It aborts on challenges, network failures or missing/invalid metrics and preserves the last successful data and date. No CAPTCHA bypass or proxy is used. Automated access can be blocked by Scholar; daily refresh is best effort, not guaranteed.

`.github/workflows/pages.yml` builds and publishes the default branch on changes, manual dispatch and daily at 06:23 UTC. It saves successful snapshots to the repository and builds with the updated values. The repository's Pages source is now **GitHub Actions**. Workflow token needs content-write access for its snapshot commit and the deployment job needs Pages permission. Protected-branch restrictions may require adapting the persistence step. The workflow and Pages deployment are enabled; the first production deployment completed successfully on 24 September 2026. GitHub may disable scheduled workflows on inactive public repositories; check Actions if the displayed date stops advancing.

## Visitor statistics — MapMyVisitors

### Current setup: flat map, published and recording

The user approved replacing the globe with the flat map on 24 September 2026. Deployment **35963745985** completed successfully. The shared footer now uses the account's official `map.js` embed with element ID `mapmyvisitors`, identifier `XwNMjhMHKSoN6RwCxjzXxGJlTJ4YQIze-VrHNEC2PLs`, `cl=ffffff` and responsive `w=a`. The old globe script is removed. A `noscript` image uses the provider's official alternative; it is not loaded alongside the JavaScript widget. The map is 380px wide on desktop and fits mobile screens.

Build/check passed for all 32 content pages and 683 internal links/assets. Desktop and 390px mobile visual checks passed. On the public homepage the map displayed **2 Pageviews**, then **3 Pageviews** after one deliberate reload; the widget has approximate location dots and links to this account's statistics page. This verifies the public widget responds to page loads. Numbers include diagnostic and preview visits and are pageviews, not unique people. The map uses the provider's default rolling-month reporting period. Chinese-network reachability has not been independently tested and still depends on access to the provider and its CDN.

Provider dashboard caution: rendered summary totals did not agree with the widget during this check (hidden/raw cell text also differed from visible text), although recent visit entries appeared. Do not quote the dashboard's raw DOM text as verified totals or claim all provider reports have reconciled. The verified end-to-end result is the live widget's 2-to-3 increase, not the dashboard summary.

### Earlier globe troubleshooting (superseded)

On 24 September 2026 the user supplied a MapMyVisitors globe embed, reopening the previously cancelled visitor-statistics request. The shared footer in `_includes/academic-home.html` now contains that exact site identifier, with HTTPS and deferred loading. `assets/css/academic-home.css` provides desktop/mobile layout. The builder reuses this footer on all 32 content pages and the 404 page; each page contains exactly one embed. The public statistics page is https://mapmyvisitors.com/web/1c8fd.

Deployment 35961458645 succeeded and the globe rendered on the public website. Desktop and 390px mobile layouts passed visual checks. Actual recording has NOT been verified: the provider dashboard still reports zero, the embedded globe keeps its default provider-home link, and its public `globe_call_home.js` request returns HTML instead of JSONP. The supplied code was checked against the logged-in provider's generated code and matches. Keep the direct statistics link available; do not claim the globe is already recording visits until the dashboard shows data. No extra tracking widget was added, to avoid duplicate counting if the provider's globe recovers.

Follow-up at 07:09 on 24 September 2026: the provider dashboard still showed Today Pageviews 0 and Total Pageviews 0. Excluded IP addresses was empty. The same-account official Map widget code uses `map.js?d=XwNMjhMHKSoN6RwCxjzXxGJlTJ4YQIze-VrHNEC2PLs&cl=ffffff&w=a` with element ID `mapmyvisitors`. The globe's `globe_call_home.js` endpoint again returned HTML; the Map widget's `widget_call_home.js` endpoint returned a valid JSONP callback with widget data. This isolates a viable alternative within the same account: the plain Map widget. No switch has been published because the user's selected presentation is the globe. A direct diagnostic request to the Map endpoint may itself register a test pageview; do not present that as proof of a genuine visitor arriving through the public site. Verify end-to-end recording after any replacement.

## Umami — parallel country/city analytics

Activated on 24 September 2026 using the owner's Umami Cloud website `3000b10a-b4af-481b-ac2b-a6a611908de4` (dashboard name: Elton, domain: hcmx1994.github.io). The shared `_layouts/academic-home.html` head loads `https://cloud.umami.is/script.js` once per page with `defer`. `data-domains` restricts tracking to the production hostname; local previews are excluded. Query strings and fragments are excluded. MapMyVisitors remains the sole source of the public visitor map and pageview count. The providers' locations and counts are not merged.

Published through PR #7, merge commit `a7e4a9790dea8a28b44f365628d0cce63c917f1e`, successful Pages deployment `36009192962`. Local build/check passed for 32 content pages and 757 internal links/assets. A separate output check confirmed exactly one Umami tracker and one MapMyVisitors widget on each of 33 rendered pages including 404. The live homepage contains the supplied Umami website ID. End-to-end verification: dashboard initially showed 0 visitors/views; after one deliberate public-homepage visit it showed 1 visitor, 1 visit, 1 view, country United Kingdom and city Wembley. This is a diagnostic visit, and the location is the provider's IP estimate, not proof of the visitor's physical location. No live browser console errors were observed. VPN-node accuracy and Chinese-network reachability remain unverified.

Owner dashboard: https://cloud.umami.is/analytics/eu/websites/3000b10a-b4af-481b-ac2b-a6a611908de4 . Use Location → Countries / Regions / Cities, or Realtime. No public share link was enabled; no API key is required for the tracking script. Umami starts collecting from activation and does not import MapMyVisitors history.

## Long-term visitor archive — public aggregates approved

On 24 September 2026 the owner selected the free workflow: periodically export Umami data and import it into an independently retained archive. `scripts/archive_visitors.py` accepts Umami's actual Country and City detail-table CSV format. It preserves visitors separately by calendar month, sums non-overlapping visits/pageviews, retains location counts, and derives shares from visits including unknown locations. Re-importing a month replaces that month rather than adding it twice. Totals, all location rows, country/city agreement, snapshot time, and completion status are validated before writing.

The first snapshot is September 2026, partial through 2026-09-24 14:26:22 UTC: 13 monthly visitors, 13 visits, 17 pageviews, 12 countries/regions and 12 cities. Both original CSV exports were downloaded from the logged-in Umami detail-table download buttons and are preserved under `local/visitor-archive/exports/2026-09/`. `files/visitor-history.json` is the canonical aggregate archive; `files/visitor-history.html` is its standalone viewer, linked from the homepage footer. Nine unit tests passed, as did the full site build/check (32 pages, 789 internal links/assets). Re-import idempotence was also checked with the real data. Browser verification confirmed country/city switching and searching preserve the 13-visit denominator (Wembley: 2/13 = 15.4%).

The owner explicitly approved publication of the historical summary to the public website/GitHub, excluding IPs and individual visit records. The archive page is linked from the shared visitor footer and is rebuilt from the canonical JSON during each site build. Only aggregate counts and location labels enter public files; raw CSV recovery copies remain under local/. There is no automatic export or recurring job: future months require manual export/import, as explained in `VISITOR-ARCHIVE.md`. This archive does not import or merge overlapping MapMyVisitors totals.

## Validation

Run `python scripts/test_update_scholar.py` and `node --check assets/js/profile-stats.js`. Start the local preview with `node scripts/preview.cjs`. The local preview serves the same metrics JSON and script as production. The complete static site now builds locally with scripts/build_site.py and is validated by scripts/check_site.py. Production deployment passed: run 35946389614, commit 212b77a8797c001d2c29bba05c9efc6e2d109a55, https://hcmx1994.github.io/.

Live verification on 24 September 2026: Scholar returned a challenge page without the metrics table. No bypass was attempted. The existing snapshot was preserved. The four parser/persistence unit tests passed; a successful live automatic refresh remains unverified.

Production verification on 24 September 2026: the first scheduled-refresh-capable workflow deployed successfully, building 32 pages and validating 683 internal links/assets. The Scholar fetch returned HTTPError; the previous snapshot and original date were preserved. Daily refresh is configured, but a successful automatic refresh remains unverified.

SerpApi integration added on 24 September 2026: scripts/update_scholar.py uses the Google Scholar Author API when the repository Actions secret SERPAPI_API_KEY is configured. It validates the returned author ID and all-time metrics, preserves old data on errors, and never writes credentials or full request URLs to logs. Eight tests passed, including malformed/quota responses, wrong author, key redaction and atomic persistence. User approved the free account; account registration/secret entry and a successful live SerpApi run are still pending.

Follow-up verification: workflow run 35947049837 (commit 36458ef008b4f0cebead5e9861f893f45eddf1d9) completed successfully. The live public-profile request succeeded this time; metrics remain 595/12/12. The failure is intermittent, not a permanent parser failure. SerpApi credentials are still pending; SerpApi has only been tested with fixtures, not a live API request. A green unit-test message mentioning SerpApi is not proof of live activation.

FINAL ACTIVATION VERIFIED on 24 September 2026: the user added SERPAPI_API_KEY to repository Actions secrets. Manual workflow 35947495722 completed successfully. The actual Refresh Scholar snapshot step in build job 107468615828 logged success via SerpApi (separately verified from unit-test output). The published site shows 595 citations, h-index 12 and i10-index 12, last verified 24 September 2026. Daily schedule remains 06:23 UTC. Registration, secret setup, live API verification and deployment are complete; no setup action remains pending.
